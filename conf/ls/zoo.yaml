# @package _global_

ls:
  seed: 0 # landscape sampler rng
  num_samples: 1 # how many samples in the landscape
  dims:
    - nn_width:
        type: Constant # the constant value will be queried from agent/zoo_optimal_ls
    - nn_length:
        type: Constant
    - learning_rate:
        type: Constant
    - neg_gamma:
        type: Constant
