# @package _global_

# For quickly checking whether stuff works
phases:
  - 500
  - 1000
agent:
  hps:
    # WARNING these overwrite values from the combo configuration!
    # learning_starts: 100 # would be 1000 for DQN, 10000 for SAC
    n_steps: 64
eval:
  freq_eval_interval: 50
